diff --git a/dist/index.js b/dist/index.js
index ba7d1e60d65c5a67c367d2619848cda168869388..82f28e5a4723b8850157580b34a0cdcb065f4dc5 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1330,7 +1330,7 @@ function hc(e, t, n) {
   } = n;
   if (!r.series.some(
     (C) => C.type !== "pie"
-  ) || !e || !t)
+  ) && !r.yAxes.some((C) => mr(r.xAxis.min) !== void 0 && mr(r.xAxis.max) !== void 0 && mr(C.min) !== void 0 && mr(C.max) !== void 0) || !e || !t)
     return;
   const c = i.canvas;
   if (!c) return;
@@ -1477,7 +1477,7 @@ function vc(e, t, n) {
   } = n, h = o.values().next().value ?? r;
   if (!i.series.some(
     (w) => w.type !== "pie"
-  ) || !e || !t)
+  ) && !i.yAxes.some((w) => mr(i.xAxis.min) !== void 0 && mr(i.xAxis.max) !== void 0 && mr(w.min) !== void 0 && mr(w.max) !== void 0) || !e || !t)
     return;
   if (!f || s <= 0 || a <= 0 || c <= 0 || m <= 0) {
     e.clear();
@@ -1928,7 +1928,7 @@ function zc(e, t) {
     interactionScales: u,
     seriesForRender: c,
     withAlpha: m
-  } = t, f = n.gridLines, h = f.show && f.horizontal.show ? f.horizontal.count : 0, p = f.show && f.vertical.show ? f.vertical.count : 0;
+  } = t, f = n.gridLines, h = f.show && f.horizontal.show ? ((F = n.yAxes[0]) == null ? void 0 : F.tickCount) ?? kc : 0, p = f.show && f.vertical.show ? s : 0;
   if (h === 0 && p === 0)
     e.gridRenderer.prepare(o, {
       lineCount: { horizontal: 0, vertical: 0 }
@@ -1974,7 +1974,7 @@ function zc(e, t) {
       );
     }
   }
-  if (l.hasPointer && l.isInGrid) {
+  if (!1 && l.hasPointer && l.isInGrid) {
     const R = {
       showX: !0,
       // Sync has no meaningful y, so avoid horizontal line.
@@ -1990,7 +1990,7 @@ function zc(e, t) {
     ), e.crosshairRenderer.setVisible(!0);
   } else
     e.crosshairRenderer.setVisible(!1);
-  if (l.source === "mouse" && l.hasPointer && l.isInGrid)
+  if (!1 && l.source === "mouse" && l.hasPointer && l.isInGrid)
     if (u) {
       const R = yr(
         c,
@@ -2296,9 +2296,35 @@ function Yc(e, t) {
           s.canvasWidth,
           s.canvasHeight
         );
-        const y = (c == null ? void 0 : c.getRange()) ?? null;
-        if ((y == null || Number.isFinite(y.start) && Number.isFinite(y.end) && y.start <= 0 && y.end >= 100) && g.sampling === "none" ? u[M] = "fullRawLine" : u[M] = "other", g.areaStyle) {
-          const S = {
+        const y = Ae(g.rawData ?? g.data), N = Ae(A), S = Number.isFinite(g.samplingThreshold) ? Math.max(1, g.samplingThreshold | 0) : 5e3;
+        y === N && N > 0 && N <= S ? e.scatterRenderers[M].prepare(
+          {
+            ...g,
+            type: "scatter",
+            data: A,
+            color: g.lineStyle.color,
+            symbolSize: 1.5
+          },
+          A,
+          r,
+          R(g),
+          s
+        ) : e.scatterRenderers[M].prepare(
+          {
+            ...g,
+            type: "scatter",
+            data: { x: [], y: [] },
+            color: g.lineStyle.color,
+            symbolSize: 0
+          },
+          { x: [], y: [] },
+          r,
+          R(g),
+          s
+        );
+        const L = (c == null ? void 0 : c.getRange()) ?? null;
+        if ((L == null || Number.isFinite(L.start) && Number.isFinite(L.end) && L.start <= 0 && L.end >= 100) && g.sampling === "none" ? u[M] = "fullRawLine" : u[M] = "other", g.areaStyle) {
+          const P = {
             type: "area",
             name: g.name,
             rawData: g.data,
@@ -2311,8 +2337,8 @@ function Yc(e, t) {
             yAxis: g.yAxis ?? "y"
           };
           e.areaRenderers[M].prepare(
-            S,
-            S.data,
+            P,
+            P.data,
             r,
             R(g),
             D
@@ -2514,6 +2540,10 @@ function qc(e, t, n, i) {
           o.canvasHeight
         );
   }
+  for (let p = 0; p < f.length; p++) {
+    const { series: F, originalIndex: R } = f[p];
+    F.type === "line" && e.scatterRenderers[R].render(s);
+  }
 }
 function Zc(e, t) {
   const {
@@ -9535,7 +9565,7 @@ function id(e, t, n) {
       (V.size > 0 || U) && (Bt(), Fn({ requestRenderAfter: !1 })), $ && ($ = !1, cn());
       const k = p.series.some(
         (Ee) => Ee.type !== "pie"
-      ), O = z;
+      ) || p.yAxes.some((Ee) => br(p.xAxis.min) !== void 0 && br(p.xAxis.max) !== void 0 && br(Ee.min) !== void 0 && br(Ee.max) !== void 0), O = z;
       if (R !== "done") {
         const Ee = Qf(p.animation), ke = (() => {
           for (let mt = 0; mt < O.length; mt++) {
