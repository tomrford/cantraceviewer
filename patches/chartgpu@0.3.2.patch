diff --git a/dist/index.js b/dist/index.js
index 06da45a4fc726b9b9e976150fa9e525565fdeb94..0b6809dafb9bfb3be30f535f3192a0cb3c201adf 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1159,7 +1159,7 @@ function _i(e, t, n) {
 function oc(e, t, n) {
   var Y, j, q;
   const { gpuContext: i, currentOptions: r, xScale: o, yScale: s, xTickValues: a, plotClipRect: c, visibleXRangeMs: f } = n;
-  if (!r.series.some((Z) => Z.type !== "pie") || !e || !t)
+  if (!(r.series.some((Z) => Z.type !== "pie") || r.xAxis.min != null && r.xAxis.max != null && r.yAxis.min != null && r.yAxis.max != null) || !e || !t)
     return;
   const g = i.canvas;
   if (!g) return;
@@ -1673,7 +1673,7 @@ function Ic(e, t) {
     interactionScales: f,
     seriesForRender: l,
     withAlpha: g
-  } = t, u = n.gridLines, y = u.show && u.horizontal.show ? u.horizontal.count : 0, p = u.show && u.vertical.show ? u.vertical.count : 0;
+  } = t, u = n.gridLines, y = u.show && u.horizontal.show ? Nc : 0, p = u.show && u.vertical.show ? s : 0;
   if (y === 0 && p === 0)
     e.gridRenderer.prepare(o, { lineCount: { horizontal: 0, vertical: 0 } });
   else if (y > 0 && p > 0 && u.horizontal.color !== u.vertical.color)
@@ -1708,7 +1708,7 @@ function Ic(e, t) {
     n.theme.axisLineColor,
     n.theme.axisTickColor,
     Nc
-  )), c.hasPointer && c.isInGrid) {
+  )), !1 && c.hasPointer && c.isInGrid) {
     const R = {
       showX: !0,
       // Sync has no meaningful y, so avoid horizontal line.
@@ -1719,7 +1719,7 @@ function Ic(e, t) {
     e.crosshairRenderer.prepare(c.x, c.y, o, R), e.crosshairRenderer.setVisible(!0);
   } else
     e.crosshairRenderer.setVisible(!1);
-  if (c.source === "mouse" && c.hasPointer && c.isInGrid)
+  if (!1 && c.source === "mouse" && c.hasPointer && c.isInGrid)
     if (f) {
       const R = lr(
         l,
@@ -1964,9 +1964,35 @@ function Lc(e, t) {
           s.canvasWidth,
           s.canvasHeight
         );
+        const d = Ne(x.rawData ?? x.data), h = Ne(v), F = Number.isFinite(x.samplingThreshold) ? Math.max(1, x.samplingThreshold | 0) : 5e3;
+        d === h && h > 0 && h <= F ? e.scatterRenderers[m].prepare(
+          {
+            ...x,
+            type: "scatter",
+            data: v,
+            color: x.lineStyle.color,
+            symbolSize: 1.5
+          },
+          v,
+          r,
+          o,
+          s
+        ) : e.scatterRenderers[m].prepare(
+          {
+            ...x,
+            type: "scatter",
+            data: { x: [], y: [] },
+            color: x.lineStyle.color,
+            symbolSize: 0
+          },
+          { x: [], y: [] },
+          r,
+          o,
+          s
+        );
         const C = (l == null ? void 0 : l.getRange()) ?? null;
         if ((C == null || Number.isFinite(C.start) && Number.isFinite(C.end) && C.start <= 0 && C.end >= 100) && x.sampling === "none" ? f[m] = "fullRawLine" : f[m] = "other", x.areaStyle) {
-          const h = {
+          const P = {
             type: "area",
             name: x.name,
             rawData: x.data,
@@ -1977,7 +2003,7 @@ function Lc(e, t) {
             samplingThreshold: x.samplingThreshold,
             connectNulls: x.connectNulls
           };
-          e.areaRenderers[m].prepare(h, h.data, r, o, R);
+          e.areaRenderers[m].prepare(P, P.data, r, o, R);
         }
         break;
       }
@@ -2082,6 +2108,10 @@ function kc(e, t, n, i) {
       } else
         s.setScissorRect(a.x, a.y, a.w, a.h), e.lineRenderers[R].render(s), s.setScissorRect(0, 0, o.canvasWidth, o.canvasHeight);
   }
+  for (let p = 0; p < u.length; p++) {
+    const { series: M, originalIndex: R } = u[p];
+    M.type === "line" && e.scatterRenderers[R].render(s);
+  }
 }
 function Uc(e, t) {
   const {
@@ -7926,8 +7956,8 @@ async function xa(e, t) {
     render: () => {
       var bo, vo, wo;
       if (cn(), !e.canvasContext || !e.canvas) return;
       ($.size > 0 || L) && (Et(), vn({ requestRenderAfter: !1 })), X && (X = !1, an());
-      const k = p.series.some((Ve) => Ve.type !== "pie"), O = U;
+      const k = p.series.some((Ve) => Ve.type !== "pie") || p.xAxis.min != null && p.xAxis.max != null && p.yAxis.min != null && p.yAxis.max != null, O = U;
       if (R !== "done") {
         const Ve = Gf(p.animation), Ze = (() => {
           for (let Gt = 0; Gt < O.length; Gt++) {
